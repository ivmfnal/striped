{
	"views":{
		"RGIDs":{
			"map":"function (doc, meta) {\n  if( meta.id.indexOf(\":@@rginfo:\") > 0 )\n  {\n    var words = meta.id.split(\":\",3);\n    emit(words[0], doc.RGID);\n  }\n}\n"
			},
		"RGInfos":{
			"map":"function (doc, meta) {\n  if( meta.id.indexOf(\":@@rginfo:\") > 0 )\n  {\n    var words = meta.id.split(\":\",3);\n    emit(words[0], doc);\n  }\n}\n"
			},
		"columns":{
			"map":"function (doc, meta) {\n  if( meta.id.indexOf(\":@@desc.json\") > 0 )\n  {\n    var words = meta.id.split(\":\",3);\n    emit(words[0], words[1]);\n  }\n}\n"
			},
		"schemas":{
			"map":"function (doc, meta) {\n  if( meta.id.indexOf(\":@@schema.json\") > 0 )\n  {\n    var words = meta.id.split(\":\",3);\n    emit(words[0], doc);\n  }\n}\n"
			},

		"data_keys":{
			"map":"function (doc, meta) {\n  if( meta.id.indexOf(\":@@data:\") > 0 )\n  {\n    var words = meta.id.split(\":\",3);\n    emit(words[0], words[2]);\n  }\n}\n"
			},

		"keys":{
			"map":"function (doc, meta) {\n  var words = meta.id.split(\":\",3);\n    emit(words[0], meta.id);}\n"
			},
		"meta":{
			"map":"function (doc, meta) {\n  emit(meta.id, meta);}\n"
			}
		}
}
		
